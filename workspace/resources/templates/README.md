# resources/templates/

Blank or starter markdown files for **new** household records. Copy or rename into `../` (for example copy `meal-plan.md` ideas into `../meal-plans/this-week.md`).

Rolling day notes (if you use them) go in [`../../memory/`](../../memory/) as `YYYY-MM-DD.md`, not in this folder.
